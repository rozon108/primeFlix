<% recievedList.forEach( movie=>{ %>

<div class="col movie-box my-2">
  <div class="box-img">
    <img src="<%= movie.poster %>" />
  </div>
 
    <div class="movie-info ">
      <span class="text-muted"><%= movie.year%></span>
      <span class="float-end"><%= movie.imdb.rating%> ⭐️</span>
    </div>
  
</div>
<% /* %>
<!-- <div class="col">
  <div class="card flex-md-row mb-4 box-shadow h-md-250">
    <div class="card-body d-flex flex-column align-items-start">
      <strong class="d-inline-block mb-2 text-primary">World</strong>
      <h3 class="mb-0">
        <a class="text-dark" href="#"><%= movie.title%></a>
      </h3>
      <div class="mb-1 text-muted"><%= movie.year%></div>
      <p class="card-text mb-auto"><%= movie.plot%></p>

      <p class="card-text mb-auto"><%= movie.imdb.rating %></p>
      <a href="#">Continue reading</a>
    </div>
    <img
      class="card-img-right flex-auto d-none d-md-block"
      data-src="holder.js/200x250?theme=thumb"
      alt="Thumbnail [200x250]"
      style="width: 200px; height: 250px"
      src="<%= movie.poster%>"
      data-holder-rendered="true"
    />
  </div>
</div> -->
<% */ %> <%}); %>
